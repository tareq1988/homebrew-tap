# Prayer Times — Homebrew Tap

```sh
brew install --cask tareq1988/tap/prayer-times
```

The current build is ad-hoc signed (not notarized). If macOS blocks it:

```sh
brew install --cask --no-quarantine tareq1988/tap/prayer-times
```

Project: https://github.com/tareq1988/prayer-times-macos
